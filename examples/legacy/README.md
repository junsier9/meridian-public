`examples/legacy/` stores archived demos that are not part of the current maintained example surface.

They are kept for reference only:
- some are heavier operational demos
- some duplicate newer maintained examples
- some are outside the immediate single-agent vertical-slice path

Only the scripts that remain in `examples/` are treated as current examples.
